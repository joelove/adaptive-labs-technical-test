<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <title>Messages</title>
  <link rel="stylesheet" href="">
</head>
<body>
  <table class="message-list">
    <th>Messages</th>
    <% @messages.each do |message| %>
    <%# binding.pry %>
    <tr class="message <%#= 'highlight' if message.highlight %>">
      <td class="message__message"><%= message.message %></td>
      <td class="message__handle"><%= message.user_handle %></td>
      <td class="message__sentiment"><%= message.sentiment %></td>
      <td class="message__counter"><%= message.count %></td>
    </tr>
    <% end %>
  </table>

  <a href="<%= messages_fetch_path %>" class="fetch-messages">Fetch more messages</a>
</body>
</html>
